fastapi==0.115.0
uvicorn==0.30.6
starlette==0.38.6
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-core==2.20.1

# sessions / signing
itsdangerous==2.2.0

# OAuth / Google login
authlib==1.3.0
requests==2.32.3
requests-oauthlib==2.0.0
google-auth==2.41.1
googleapis-common-protos==1.71.0

# OpenAI client + audio stuff
openai==1.43.0

# file uploads
python-multipart==0.0.9
Pillow==10.4.0

# misc utils and perf
anyio==4.11.0
typing-extensions==4.15.0
orjson==3.11.4
httpx==0.27.0

# DB doesn't need extra package (sqlite3 is built-in)

# optional stuff you already had in your project (we'll leave them in because you were using them earlier)
tqdm==4.66.5
python-dateutil==2.9.0.post0
